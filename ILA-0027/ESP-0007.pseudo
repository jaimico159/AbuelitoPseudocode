INICIO
  boton = BotonNotificaciones
  SI(usuario.click(boton)){
    mostrarListaDesplegable(index)
  } SINO {
    cerrarListaDesplegable()
  }
  FIN SI
FIN

def index
  notifications = Notification.where(status: 'active')
  respond_to do |format|
    format.json { json: notifications }    
  end
end
