INICIO
  SI(usuario.realizarVenta){
    checkStock()
  }
  FIN SI
FIN

def checkStock()
  if kardex_header.stock <= kardex_header.minimun_stock
  notification = NotificationTemplate.find_by_name('minimun_stock')
  notification
  respond_to do |format|
    format.json { json: notifications }    
  end
end
