INICIO
  boton = Menu.BotonNotificaciones
  SI(usuario.click(boton)){
    desplegarLista()
  }
  FIN SI

FIN

function desplegarLista(){
  notificaciones = NotificationTemplates.all
  render :json notificaciones
}